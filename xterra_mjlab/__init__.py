"""xTerra RL Lab (mjlab) — locomotion reinforcement learning on mjlab / MuJoCo-Warp.

Import :mod:`xterra_mjlab.tasks` to register the tasks, then use the mjlab
train/play entry points (see ``scripts/``).
"""

from pathlib import Path

XTERRA_MJLAB_ROOT = Path(__file__).parent
"""Filesystem root of the installed package."""
