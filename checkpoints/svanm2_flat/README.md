# SvanM2 flat velocity policy

Pretrained policy for `xTerra-Mjlab-Velocity-Flat-SvanM2`. See `../../MODEL_CARD.md`.

| File | Use |
|------|-----|
| `svanm2_flat.pt` | rsl_rl checkpoint — `play.py ... --checkpoint-file ...` |
| `policy.onnx`   | ONNX actor (270→12, 6-frame history) — deployment |
